// oText - Step 

letters += spd;
text_current = string_copy(text,1,floor(letters));

draw_set_font(FSign);
if (h == 0) h =string_height(text);
//w = string_width(text_current);

if (letters >= length) and (keyboard_check_pressed(vk_enter))
{
    instance_destroy();
    with(Camera) follow = oMonkey;
}